module.exports = {
  printWidth: 120, // Set the maximum line width to 120 characters
  tabWidth: 2, // Use 2 spaces for indentation
  useTabs: false, // Use spaces instead of tabs
  semi: true, // End statements with a semicolon
  singleQuote: true, // Use single quotes instead of double quotes
  trailingComma: 'all', // Add a trailing comma wherever possible (e.g., in objects, arrays, etc.)
  bracketSpacing: true, // Print spaces between brackets in object literals
  jsxBracketSameLine: false, // Put the `>` of a multi-line JSX element at the end of the last line instead of being alone on the next line
  arrowParens: 'always', // Always include parentheses around arrow function arguments
  endOfLine: 'auto', // Maintain existing line endings (mixed line endings within one file are normalized by the configured `endOfLine` value)
};
